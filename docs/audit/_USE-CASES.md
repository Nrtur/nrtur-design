# nrtur CRM — Use-Case Validation (Phase 4)
_Generated by the crm-gap-audit skill · 2026-07-01 · Phases 1–4 (audit only, no code changed) · source: index.html (21,483 lines)_

Each core CRM workflow traced end-to-end through the actual screens. ✅ fully supported · ⚠️ partial · ❌ broken.

## Summary

| # | Use case | Result |
|---|---|---|
| 1 | Capture a lead (form / manual / import) so a lead exists with a lifecycle status | ⚠️ partial |
| 2 | Assign a lead to an owner | ✅ supported |
| 3 | Work & qualify a lead: log activities and advance its lifecycle status | ⚠️ partial |
| 4 | Convert a qualified lead into Contact + Company + Deal | ⚠️ partial |
| 5 | The new deal enters the pipeline at the first stage — a newly created deal (from lead conversion or manual create) should land as a card in the pipeline board's FIRST stage (Prospecting). | ⚠️ partial |
| 6 | Move a deal through pipeline stages | ⚠️ partial |
| 7 | Close a deal Won or Lost with a reason | ❌ broken |
| 8 | Post-sale: the Contact/Company persists and can hold future deals — i.e. after a sale/conversion, the Contact and Company remain real records in the CRM, and a user can open a NEW deal against them later that correctly attaches to those same records. | ⚠️ partial |
| 9 | Log an activity / note / task against any record and see it in that record history | ⚠️ partial |
| 10 | See all deals for a given Contact and for a given Company | ⚠️ partial |
| 11 | Filter and search each entity list (leads, contacts, companies, deals) | ⚠️ partial |
| 12 | Reassign ownership of a record (change the Owner of a Contact / Lead / Company / Deal / Custom record) | ⚠️ partial |
| 13 | Reporting: pipeline value by stage, lead→deal conversion rate, win/loss, activity volume — traced through the Reports page (ReportsPage@11723) which reuses the dashboard widget engine (WIDGET_DEFS@3792, renderWidgetBody@4537). | ⚠️ partial |
| 14 | Handle a lost / disqualified lead without losing the record | ⚠️ partial |


---

## 1. Capture a lead (form / manual / import) so a lead exists with a lifecycle status — ⚠️ partial

| Step | Screens touched | Status | Note |
|---|---|---|---|
| Open the capture entry point (QuickAdd '+' menu, or the 'New Lead' button on the Leads list) | QuickAddMenu SINGLE list item 'Lead' @2037; MULTI 'Leads' @2047; 'Bulk import' submenu 'Import leads' @2082; Leads list 'New Lead' button @8788 | ✅ supported | 'Lead' is a first-class option in every entry path: single record (2037), spreadsheet/multiple (2047), and bulk import (2082). The Leads-list button routes through the same QuickAdd drawer via window.__nrturQuickAdd('lead') @8788/21242. |
| MANUAL (single) — fill the create form and save | AddRecordDrawer @2336 (fields from arCreateFields @2162, Status field bound to LEAD_STATUSES); doCreate @2361 -> arBuildRecord @2326 -> persist @2360 (crm.setLeads) | ✅ supported | Fully wired. arBuildRecord (2326) builds a real lead object, defaulting/validating status against LEAD_STATUSES ('New' fallback), and persist() calls crm.setLeads. Owner/source/estValue captured. This is the primary working manual path. |
| MANUAL (multiple / spreadsheet) — paste rows and Create all | AddRecordDrawer 'Multiple' mode @2406; RecordSheet onCreateRows @2408 -> arBuildFromSheet @2333 -> crm.setLeads | ✅ supported | arBuildFromSheet (2333) sets a LEAD_STATUSES-validated status per row and setLeads persists. Works. |
| IMPORT (CSV/TSV/JSON) — upload, map, review, run | BulkImportWizard @5077; runImport @5118 -> importBuildRecord @5049 -> apply(crm.setLeads) @5134; ImpStepper @5065 | ✅ supported | Four-step wizard with dedupe (skip/update). importBuildRecord (5054) validates status against LEAD_STATUSES ('New' fallback) and setLeads persists. Fully functional. |
| FORM — build/submit an inbound web form that creates a lead | FORMS_DATA onSubmit.recordType:'lead' @16597; onSubmit editor 'Lead' tile @16906; FormPreview @16725 (inputs + Submit rendered disabled @16777-16786); static FORM_SUBMISSIONS log @16633 | ❌ broken | The form's lead-capture logic is CONFIGURATION ONLY. FormPreview renders every input and the Submit button as disabled, and NO code path in the Forms module reads onSubmit.recordType to create a record — there is zero setLeads/setContacts call anywhere in forms. 'Lead created' events shown are the hardcoded FORM_SUBMISSIONS array (16633). The form path never produces a lead in CRM state. |
| Lead persists and surfaces with a LIFECYCLE status (not a deal pipeline) | Leads state @21146; Leads list status chip via LEAD_STATUS_CHIP @8438; LeadDetailPage status pill leadPrimary @8868; onLeadField/updateLead @8878/21214 | ✅ supported | Captured lead appears in the Leads list and detail with an editable status pill (New/Contacted/Nurturing/Qualified/Unqualified) persisted via updateLead. Correctly a lifecycle status with NO pipeline/stage on the lead. Caveat: 'Qualified' is a fused value shared with the Deal pipeline stage (LEAD_STATUSES @8437 vs STAGES_DATA 'qualified' @7376). |
| AddLeadPage full-page form (fallback when QuickAdd global not mounted) | AddLeadPage @8944; 'Create lead' button @8964 -> goTo('lead-detail') with NO setLeads | ❌ broken | The standalone add-lead page (reached at route 'add-lead' @21282, and the goTo fallback on the Leads 'New Lead' button @8788) is a dead shell: its 'Create lead' button just navigates to lead-detail (8964) and persists nothing. Only reachable when window.__nrturQuickAdd is unmounted, but it is a genuinely non-functional create screen. |


---

## 2. Assign a lead to an owner — ✅ supported

| Step | Screens touched | Status | Note |
|---|---|---|---|
| Open the Leads list and reassign owner inline via the Owner column avatar dropdown | LeadsPage / LEAD_COLS owner column @8682 -> ContactOwnerDropdown @4950 -> setOwner @8737 | ✅ supported | The Owner column renders ContactOwnerDropdown (avatar button, click to open a 'Reassign to' menu listing TEAM_MEMBERS @4955). Selecting a member calls colCtx.onOwner -> setOwner(id,m) @8737, which does setRows(...owner:m.avatar, ownerColor:m.color) and toasts 'Reassigned to <name>'. Real, persisted state write. Gated read-only when !effCanObject('Leads','edit') (@4952/8767). |
| Select multiple leads and use the BulkBar 'Assign owner' action | BulkBar @6208 rendered for leads @8809; Assign owner button @6239; assignOwner @6217 | ✅ supported | BulkBar (object='lead') shows an 'Assign owner' button @6239 listing TEAM_MEMBERS; assignOwner(m) @6217 calls patchSel({owner:m.avatar,ownerColor:m.color}) -> setRows over every selected id, then toasts 'Owner set to <name> on N leads'. Gated by canEdit=effCanObject('Leads','edit') @6233. Genuinely persists. |
| Open a Lead detail page and change the Owner property inline | LeadDetailPage @8853; leadPrimary Owner field @8868 -> POwner @6445 -> onLeadField @8878 -> updateLead @21214 | ✅ supported | Owner is a first-class field in leadPrimary (type:'owner' @8868), rendered by RecordProperties/PField ('owner' case @6486) as POwner @6445. Committing calls onLeadField('owner',avatar) -> updateLead(l.id,{owner}) @21214 which setLeads-maps the record. POwner also allows 'Unassigned' (empty option @6447). Read-only honored via readOnly={!effCanObject('Leads','edit')} @8914. |
| Alternatively edit Owner through the Lead detail Edit drawer | RecordEditDrawer via editGroups @8875; onEditSave @8874 -> updateLead | ✅ supported | 'Edit lead' in the More menu @8896 opens RecordEditDrawer with leadPrimary (incl. Owner) in editGroups. onEditSave(patch) -> updateLead(l.id,patch). Persisted. |
| Assign the owner at lead creation time via the QuickAdd drawer | AddRecordDrawer @2336; arDefaultFields lead l_owner @2149; AROwner @2266; arBuildInitial @2305; arBuildRecord @2326 | ⚠️ partial | QuickAdd (window.__nrturQuickAdd('lead') @21242) is the real create path. The lead create schema includes an Owner field (l_owner @2149) rendered as AROwner, and arBuildRecord writes owner+ownerColor via arOwnerColor @2326 -> persist -> setLeads @2360. Works, BUT owner can never be truly Unassigned here: arBuildInitial defaults it to TEAM_MEMBERS[0].avatar @2309 and arBuildRecord falls back to 'AM' @2323. Smell: the same lead create form also carries an l_pipeline field (Sales/Onboarding Pipeline @2149) — a deal-pipeline concept on a Lead. |
| Assign owner on the standalone Add Lead page | AddLeadPage @8944; Owner <select> @8962; 'Create lead' button @8964 | ❌ broken | The page has an Owner <select> populated from TEAM_MEMBERS @8962, but the whole form is a dead shell: inputs are uncontrolled and 'Create lead' just calls goTo('lead-detail') @8964 with no setLeads and no owner capture. No lead is created and the chosen owner is discarded. (Most 'New Lead' buttons route to QuickAdd first @8788, so this page is a fallback, but it is reachable and non-functional.) |


---

## 3. Work & qualify a lead: log activities and advance its lifecycle status — ⚠️ partial

| Step | Screens touched | Status | Note |
|---|---|---|---|
| Open the Leads list and pick a lead to work | LeadsPage @8706 (list, filters, views); LeadRow @8538 opens LeadDetailPage @8854 | ✅ supported | Full list with LEAD_STATUSES chips, score, owner. Clicking a row (or RecordSwitcher @8888) opens the detail page. Non-deleted leads only. |
| Advance the lead's lifecycle status (New -> Contacted -> Nurturing -> Qualified / Unqualified) | LeadStatusDropdown @8536/@8909 on detail; inline LeadStatusDropdown @8545 on the list row | ✅ supported | Detail dropdown @8909 calls updateLead(l.id,{status:s}) which persists via the reducer @21214 and also mirrors to local status state. List inline @8736 setStatus persists to state + toast. Options come from LEAD_STATUSES @8437 = New/Contacted/Nurturing/Qualified/Unqualified. This is a true lifecycle status on the Lead (no pipeline board) — correct ownership. Only smell: 'Qualified' collides with the Deal pipeline 'Qualified' stage (@7376/@7969), the classic fused value. |
| Log a NOTE against the lead | ActivityTimeline @8930 -> TimelineComposer Note tab @7061 -> addNote @8861 | ⚠️ partial | The composer renders (onAddNote is passed @8930) and adding a note prepends mkAct('note') into page-local `extra` React state @8861. It is never written to any store, so it VANISHES on navigation/refresh. Notes cannot be edited or deleted. 'Activity history' is a presentational mock — the base feed is regenerated by actLeadOwn @6742 on every render. |
| Log a CALL or EMAIL against the lead | ActivityTimeline @8930 (props); TimelineComposer tabs @7055-7057 | ❌ broken | On the lead detail the ActivityTimeline is given only onAddNote and onSchedule (@8930) — NOT onEmail or onLogCall. TimelineComposer only pushes the Email/Call tabs when those props exist (@7055-7056), so on a Lead there is NO UI to log a call or an email. Call/email/SMS entries that appear in the timeline are demo-synthesized by actLeadOwn @6746-6749, not user-loggable. So 'log activities' for calls/emails has no screen for a Lead. |
| Add / complete a TASK for the lead (follow-up work) | RecordTasks @8929 -> TaskFormModal @7130; match (t)=>t.contact===l.name && t.linkType==='lead' @8929 | ✅ supported | Tasks are real, persisted to the shared CalendarContext store (addItem/toggleDone @7108). Add-task prefills {contact:l.name,linkType:'lead',company:l.company} @8929. Completing/creating fires onLog which drops an (ephemeral) timeline event. Link is name-string + linkType, so a lead and contact of the same name are disambiguated only by linkType — fragile but functional. |
| Schedule a call/meeting on the lead | Schedule button @8892 -> CalEventFormModal @8940 -> calAddItem | ✅ supported | Opens CalEventFormModal prefilled {type:'call',contact:l.name,linkType:'lead',company:l.company} @8940; saved via calAddItem to the shared calendar store, and an ephemeral 'meeting' activity is pushed to `extra`. |
| See the lead's engagement history / qualification context | ActivityTimeline kind='lead' @8930 (actLeadOwn @6742); QualBadge @8909; QualificationCard @8915 | ⚠️ partial | Timeline shows a plausible history but it is ENTIRELY demo-generated per render (actLeadOwn @6742), never persisted, and — unlike contact/company/deal — the lead feed pulls NO cross-linked records (buildActivityFeed lead branch @6785 returns only its own acts). QualBadge/QualificationCard only render when l.qual exists (seeded on one lead) and reflect qual.status, a separate embedded qualification artifact, not the editable lifecycle status. |


---

## 4. Convert a qualified lead into Contact + Company + Deal — ⚠️ partial

| Step | Screens touched | Status | Note |
|---|---|---|---|
| 1. Open a qualified lead — from the Leads list (LeadsPage, list rows LeadRowDyn/LeadRow @8538) click a row to reach Lead detail (LeadDetailPage @8853), or act directly from the list row. | LeadsPage list @8773; LeadRow @8538; LeadDetailPage @8853 | ✅ supported | Seed leads with status 'Qualified' exist (Olivia @8444, Aisha @8448, Hannah @8450). Both list and detail open cleanly and are real screens. |
| 2. Trigger Convert — per-row 'Convert' pill (LeadRow onConvert @8549 -> setConvertLead @8767 -> ConvertLeadModal @8845), or detail 'Convert now' (@8925 -> setConvertOpen -> ConvertLeadModal @8938). | LeadRow Convert pill @8549; LeadDetail 'Convert now' @8925; ConvertLeadModal @8573 | ✅ supported | Both single-record entry points are real and open the same working ConvertLeadModal. |
| 3. Review/edit the three records in the Convert modal — Contact card (always), Company card (only if lead.company set @8617, else skipped @8634), Deal card (optional toggle, defaults on when estValue>0 @8580). Fields editable, pre-filled from the lead. | ConvertLeadModal @8573-8659; contact @8609; company @8625; deal @8646 | ✅ supported | Controlled inputs, expand-to-edit; Company auto-skips with no company name (@8636); Deal is genuinely optional (@8643). |
| 4. Confirm 'Convert lead' — submit @8590 -> doConvert @8740 -> buildConvertRecords @8556 creates and PERSISTS Contact (+Company if name, +Deal if toggle) via setContacts/setCompanies/setDeals @8740; lead flipped converted:true and locked read-only. | doConvert @8740; buildConvertRecords @8556; ConvertedLeadRow @8661 | ✅ supported | Real state mutation, id back-links set on the lead (@8740), success toast. The single-record path genuinely works. |
| 5. New records joined to each other by id — new Contact should point to new Company, new Deal to new Contact/Company. | buildConvertRecords contact @8562, company @8564, deal @8566 | ❌ broken | Created trio is linked only by NAME string, never id. Contact stores co:lead.company (@8562) with no convertedToCompanyId; Deal stores company + primaryContact as name strings (@8566). Freshly-created records share no key. |
| 6. Jump to new records from the converted lead — ConvertedLeadRow @8667-8669 and detail card @8925 deep-link via convertedToContactId/CompanyId/DealId. | ConvertedLeadRow @8661; LeadDetail converted card @8925 | ⚠️ partial | Works for runtime conversions (ids set @8740). BROKEN for pre-seeded converted lead Sofia Marchetti (id:3 @8446): has conv* name strings but NO convertedTo*Id, so goTo receives undefined. |
| 7. Bulk convert from the Leads list — select leads, BulkBar 'Convert' @6255. | BulkBar Convert @6255 | ❌ broken | Toast-only: onClick just showToast('...queued for conversion') @6255. No records created, converted flag never set — diverges from real doConvert @8740. |
| 8. Deal lands in a pipeline stage — deal @8566 sets stageKey:'prospecting', dStage:'Prospecting', pipeline (default 'Sales Pipeline'). | buildConvertRecords deal @8566; modal pipeline select @8650 | ⚠️ partial | Pipeline correctly lives on the DEAL (good). But deal.pipeline is a name string never reconciled with stageKey; the board filters by stageKey only, so an 'Onboarding Pipeline' converted deal still renders under Sales stages. |


---

## 5. The new deal enters the pipeline at the first stage — a newly created deal (from lead conversion or manual create) should land as a card in the pipeline board's FIRST stage (Prospecting). — ⚠️ partial

| Step | Screens touched | Status | Note |
|---|---|---|---|
| User triggers deal creation. Two real entry points exist: (A) Lead conversion — Leads list/detail row 'Convert' opens ConvertLeadModal (ConvertLeadModal@8573; row action onConvert@8767 sets convertLead; doConvert@8740). (B) Manual create — the primary 'New Deal' button on the pipeline board (@7851) and the per-stage 'Add deal' footer (@7872) call window.__nrturQuickAdd('deal',...) (@21242) which opens AddRecordDrawer (@2336). | Leads list/detail Convert button (@8767); ConvertLeadModal (@8573); Pipeline board 'New Deal' (@7851) + per-stage 'Add deal' (@7872); AddRecordDrawer (@2336) | ✅ supported | Both entry points are wired to real, persisting create paths. Note the standalone AddDealPage (@8049, route 'add-deal') is a DEAD fallback — its 'Create deal' button just goTo('deal-detail') (@8069) with no setDeals — but it is only reached if window.__nrturQuickAdd is undefined, which it isn't at runtime, so the live paths bypass it. |
| The deal record is built with a stage. Conversion path: buildConvertRecords (@8556) hardcodes stageKey:'prospecting', dStage:'Prospecting' (@8566) — correctly the first stage. Manual path: arBuildRecord (@2325) reads the drawer's Stage field and maps its value to a stageKey via keyMap; it defaults to 'Qualified'/'qualified' when no stage is chosen. | buildConvertRecords@8566 (conversion); arBuildRecord@2325 + arBuildInitial@2314 (manual drawer) | ⚠️ partial | CONVERSION is correct — deal always starts in 'prospecting' (first stage), and the modal even labels it 'Placed in first stage' (@8651). MANUAL create is WRONG: the deal Stage field options are ['Qualified','Proposal','Negotiation','Won','Lost'] (arDefaultFields@2137 and property schema d_stage@14055), with Prospecting OMITTED. arBuildInitial defaults the field to options[0]='Qualified' (@2314), and arBuildRecord falls back to 'qualified' (@2325). So a manually created deal enters at the SECOND stage (Qualified), never the first. |
| The deal is persisted into the shared deals[] state. Conversion: doConvert calls setDeals(a=>[deal,...a]) (@8740). Manual: AddRecordDrawer persist() calls crm.setDeals(a=>[rec,...a]) (@2360/2364). Both write to the single deals state (@21152) that the pipeline board reads. | doConvert@8740; AddRecordDrawer.persist@2360; deals state@21152 | ✅ supported | Both paths mutate the same live deals array via CrmDataContext (crm.setDeals), so a created deal is immediately visible to the board with no refresh needed. |
| The pipeline board renders the deal in its stage column. PipelinePage (@7678) builds the Sales Pipeline from STAGES_DATA (@7715) — stages render in order with Prospecting first (@7371, board columns @7866). dealsFor(stage) (@7767) filters _deals.filter(d=>d.stageKey===stage.key), so the card appears in the column whose key matches its stageKey. | PipelinePage board columns@7866; dealsFor@7767; first stage 'prospecting'@7371 | ⚠️ partial | The board correctly places a card by stageKey. A converted deal (stageKey 'prospecting') lands in the first column as intended. A manually created deal (stageKey 'qualified') lands in the second column — so for the manual path the card enters the pipeline but NOT at the first stage. Also: the per-stage 'Add deal' footer under Prospecting passes prefill {stage:'Prospecting'} (@7872), but buildInitial only applies it if options.includes('Prospecting') (@2345) — which is false — so even the Prospecting-column add button silently produces a 'Qualified' deal. |


---

## 6. Move a deal through pipeline stages — ⚠️ partial

| Step | Screens touched | Status | Note |
|---|---|---|---|
| Open the Pipeline board and select the pipeline (deal pipeline) whose deals you want to move | PipelinePage @7678; PipelineSwitcher @7551/@7824 (lists Sales Pipeline + Customer Onboarding + any object/custom aux pipelines); allPipelines @7741; default activeId=1 Sales Pipeline @7723 | ✅ supported | Board renders real deals grouped by stage. Only 'Sales Pipeline' (id:1, built from STAGES_DATA @7715) contains REAL deals; 'Customer Onboarding' (id:2 @7716-7721) and any duplicated pipeline hold only demo[] cards (fake, non-openable). Switching to a Lead/Contact/Company/Custom pipeline routes to ObjectBoard @7810 — that is NOT a deal, it groups records by a status/type field. |
| Drag a deal card from one stage column to the next stage on the board | KanbanBoard board view @7860-7885; onMove @7884 -> moveDeal/applyMove @7765-7766; setDeals writes stageKey:toStage.key @7765; footer hint 'Drag deals between stages to move them' @7924 | ✅ supported | Real persistence: for a real->real stage move, setDeals updates the deal's stageKey in live React state (@7765). Gated by effCanObject('Deals','edit') (@7865/@7884); no permission = silently ignored. Demo-card pipelines only reshuffle stage.demo[] arrays (@7765 else-branch), not real deals. |
| Move a deal into the terminal Won or Lost stage and record the outcome reason | moveDeal detects kind via /lost/ or /won\|live/ regex on stage name @7766/@7884 -> setOutcomePrompt; DealOutcomeModal @7465-7491 (win/loss reason grid + optional note); modal render+onConfirm @7927 | ⚠️ partial | The stage move itself persists (applyMove @7765). BUT the captured win/loss REASON and NOTE are thrown away: onConfirm @7927 calls applyMove then only interpolates `reason` into a toast string — it never calls updateDeal or stores reason/note anywhere. There is also no durable Won/Lost status field; won/lost is inferred solely from stageKey. Loss reason (a core CRM data point) is unrecoverable after the toast fades. |
| Alternatively, open the deal detail page and advance it with the stage ladder / 'Move to next stage' button | DealDetailPage @7971; DEAL_STAGE_ORDER ladder @8029; 'Move to {nextStage}' button @8011; stage dots @8029; onDealField dStage map @7991; RecordProperties Stage pill @7992/@8032 | ⚠️ partial | Works for forward moves but is driven by an INDEPENDENT stage list DEAL_STAGE_ORDER=['Prospecting','Qualified','Proposal','Negotiation','Won'] (@7969) that OMITS 'Lost' — you can never step a deal to Lost from the ladder or the 'Move to next stage' button. The ladder is also hardcoded to the Sales-pipeline stage names, so a deal in a user-created pipeline (custom stage keys) shows a mismatched ladder. drec.dStage synthesizes 'Qualified' for any prospecting deal (@7990), mislabeling a Prospecting deal as Qualified on the detail Stage pill. Won/Lost here uses a plain confirm() dialog (@8029) that captures NO reason at all — inconsistent with the board's DealOutcomeModal. |
| Alternatively, change a deal's stage from the List view inline dropdown, or bulk-move selected deals | List view @7886-7898; DealStageDropdown @7494/@7507; lvStage @7805 -> applyMove; BulkBar onStageMove=onBulkStage @7808/@7888 | ✅ supported | Inline single-deal stage change and multi-select bulk stage move both call applyMove and persist stageKey. Note bulk/inline Won/Lost moves bypass the DealOutcomeModal entirely (applyMove called directly), so no outcome prompt at all on those paths — inconsistent with board drag. |
| See the move reflected in stage counts, pipeline value, weighted forecast, and the deal's activity history | dealsFor/fc_stageRows rollups @7767/@7784; topbar metrics @7776/@7832; Forecast view @7903-7921; ActivityTimeline on detail @8036 | ⚠️ partial | Stage counts, per-stage value, and weighted forecast recompute live from stageKey (@7784) — correct. But the top METRICS Won/Avg/Win-rate cards (@7778-7780) are HARDCODED strings ($134k/$11.5k/68%), not derived, so 'Won' does not update when you close a deal. The activity timeline's stage-change events are demo-synthesized per render (mkAct), not written by applyMove — moving a deal creates NO real, persisted stage-change history entry. |


---

## 7. Close a deal Won or Lost with a reason — ❌ broken

| Step | Screens touched | Status | Note |
|---|---|---|---|
| Open the deal being closed — on the Pipeline board (Kanban) or the Deal Detail page | PipelinePage/ObjectBoard Kanban (KanbanBoard@7531); DealDetailPage@7971 | ✅ supported | Deals are reachable from the board and the detail page; RecordSwitcher@8008 lets you move between deals. |
| Move the deal into the terminal Won or Lost stage | Board drag: moveDeal@7766 → applyMove@7765. Detail: header 'Move to' button@8011 + stage-ladder dots@8029 | ⚠️ partial | Board drag works and detects Won/Lost by regex on the STAGE NAME (/lost/, /won\|live/ @7766) — a custom pipeline whose terminal column isn't literally named Won/Lost/Live never triggers the close flow. On the Detail page DEAL_STAGE_ORDER@7969 OMITS 'Lost', so the ladder (8029) and 'Move to {nextStage}' button (8011) can advance to Won but can NEVER reach Lost; the only way to set Lost from detail is the edit-drawer dStage pill (7992). |
| Be prompted to capture a Win/Loss reason | Board: DealOutcomeModal@7465 (reason grid@7481 + optional note@7483). Detail: openConfirm@8029 (plain confirm, NO reason). Edit drawer dStage pill@7992 (NO confirm at all) | ⚠️ partial | ONLY the board-drag path shows a reason picker. The Deal Detail ladder/button uses a generic yes/no openConfirm with no reason field (8029), and setting Lost via the edit drawer (7992/7997) fires no confirmation and no reason prompt. |
| Persist the chosen reason (and note) on the deal record | DealOutcomeModal onConfirm@7927 → applyMove@7765; updateDeal@21216 | ❌ broken | THE CORE BREAK. Even on the board path that DOES collect a reason, onConfirm@7927 calls applyMove (which writes ONLY stageKey — line 7765) plus a toast. The modal's onConfirm signature is (reason)=>… so the optional NOTE (7483) is never even read, and the reason is used solely to build the toast string. Neither reason nor note is written via updateDeal — no lossReason/winReason/outcomeNote field exists on a deal (grep confirms none). The data is discarded the instant the toast fires. |
| Record Won/Lost as a durable deal status | drec.dStatus derivation@7990; STAGES_DATA stageKey 'won'/'lost'@7370-7393 | ❌ broken | There is NO stored status field. Won/Lost is inferred at render solely from stageKey==='won'/'lost' (dStatus recomputed @7990, readOnly). Reopening = just moving stageKey back. No close-date stamp, no closed-by, no immutable outcome record. |
| Surface the reason afterward (timeline / deal view / reporting) | actDealOwn won/lost events@6727-6728; ActivityTimeline@8036 | ❌ broken | The won/lost timeline events (6727-6728) are hardcoded generic strings ('closed-won.' / 'Marked closed-lost.') with NO reason interpolated — the captured reason never appears in history. The DealOutcomeModal loss/win reason lists (7468-7469) feed nothing downstream: no reason column, no loss-reason report, no win-rate-by-reason. The reason is unrecoverable after the toast disappears. |


---

## 8. Post-sale: the Contact/Company persists and can hold future deals — i.e. after a sale/conversion, the Contact and Company remain real records in the CRM, and a user can open a NEW deal against them later that correctly attaches to those same records. — ⚠️ partial

| Step | Screens touched | Status | Note |
|---|---|---|---|
| 1. Contact/Company persist as real records after conversion. Lead Convert (ConvertLeadModal@8573, doConvert@8740) calls buildConvertRecords@8556 and does setContacts([contact,...])/setCompanies([company,...]) — the records land in live React state (contacts@21145, companies@21147) and show in the Contacts list (ContactsPage@6263 reads live rows@6269) and Companies list. Records genuinely persist for the session. | ConvertLeadModal@8573, doConvert@8740, buildConvertRecords@8556, ContactsPage@6263, CompanyDetailPage@8312 | ✅ supported | Contact always created; Company created only if lead.company is non-empty (buildConvertRecords@8563-8564). Persistence is session-only (in-memory state), which is expected for a static prototype. |
| 2. Open the persisted Contact's detail page to work it post-sale (ContactDetailPage@7193, c=contacts.find(x=>x.id===nav)@7197). | ContactDetailPage@7193 | ✅ supported | Live contact resolved by id from state. Header, properties, activity timeline render for the real record. |
| 3. See the Contact's existing/held deals on its detail page (the 'Deals · 2' panel@7302-7305). | ContactDetailPage 'Deals · 2' panel@7302-7304 | ❌ broken | The panel is fully HARDCODED: the count 'Deals · 2'@7303 and the two cards (Q3 Engagement $24k / Annual Retainer $48k, stages Proposal/Qualified)@7304 are literals identical for EVERY contact, and each card's onClick=goTo('deal-detail') passes NO id. A post-sale contact with zero or three real deals still shows exactly these two fake ones; a contact's real converted deal never appears here. |
| 4. Create a NEW (future) deal against the persisted Contact via the panel's 'New deal' button@7303 (window.__nrturQuickAdd('deal','single',{contact:c.name,company:c.co})). | 'New deal' button@7303 -> __nrturQuickAdd@21242 -> AddRecordDrawer@2336 -> arBuildRecord deal branch@2325 | ⚠️ partial | The deal DOES persist (persist()->setDeals@2360). Prefill maps contact/company into Lookup fields by NAME only (buildInitial@2345), and arBuildRecord stores deal.primaryContact and deal.company as NAME STRINGS@2325 — never the contact's/company's id. The link is name-string, so it works only while names stay unique and unchanged. |
| 5. Confirm the newly-created future deal shows back on the Contact detail page. | ContactDetailPage 'Deals · 2' panel@7302-7304 | ❌ broken | There is no code path that lists a contact's real deals on this panel — it is the hardcoded literal from step 3. So a deal you just created against this contact will NOT appear on the contact's own page. (The ActivityTimeline@7320 does derive deals by name via buildActivityFeed@6767, so the deal's activity may surface in the timeline, but the visible Deals panel and the timeline disagree — the panel is fake.) |
| 6. Open the persisted Company detail page and see its held deals (CompanyDetailPage@8312, dl=dealsForCompany(co.name)@8319, Deals section@8384-8390). | CompanyDetailPage@8312, dealsForCompany@8087, Deals list@8384-8390 | ⚠️ partial | Unlike the Contact panel, the Company Deals list is REAL: dealsForCompany@8087 calls allDeals() which reads live _liveDeals@8085/21153, filtering deal.company===co.name. A future deal created with the matching company NAME does appear here. BUT the join is name-string: dealsForCompany open-count treats stageKey!=='won' as open, so LOST deals count as open@8087, and any company-name typo/rename detaches the deal. |
| 7. Create a NEW (future) deal against the persisted Company via the header/section 'New deal' button@8345/@8385 (__nrturQuickAdd('deal','single',{company:co.name})). | 'New deal' buttons@8345,8385 -> AddRecordDrawer@2336 -> arBuildRecord@2325 | ⚠️ partial | Deal persists and, because dealsForCompany matches on the prefilled company NAME, it will show in the Company's Deals list. Link is still name-string (deal.company), no companyId written@2325 — fragile to rename and to duplicate company names (Meridian Agency vs Meridian Agency Inc.). |
| 8. Open that future deal's detail page and have its Primary contact / Company links point back at the persisted records (DealDetailPage@7971, dealContact=contactsForCompany(d.company)[0]@7975, companyRec=COMPANIES_DATA.find@7976). | DealDetailPage@7971-7976, primaryContact/company lookups@7992 | ❌ broken | dealContact reads the STATIC CONTACTS_DATA constant (contactsForCompany@8086) and picks the company's FIRST seed contact — NOT the live contact you attached and NOT necessarily deal.primaryContact. companyRec reads the STATIC COMPANIES_DATA@7976. A company/contact created at runtime (post-conversion) is absent from those frozen constants, so the deal's 'Company'/'Primary contact' links resolve to a stale seed record or to undefined (dead link), never to the persisted post-sale record. |


---

## 9. Log an activity / note / task against any record and see it in that record history — ⚠️ partial

| Step | Screens touched | Status | Note |
|---|---|---|---|
| Open a record detail page (Contact/Company/Lead/Deal/Custom record) that hosts the history | ContactDetailPage@7193, DealDetailPage@7971, CompanyDetailPage(addNote@8324), LeadDetailPage(addNote@8861), CustomRecordDetailPage@20770 | ✅ supported | All five detail-page types render both a RecordTasks panel and an ActivityTimeline. The timeline is the 'record history' surface — header 'Activity · N events · own + linked records' @7085. |
| Add a NOTE via the timeline composer | TimelineComposer@7049 (textarea id 'cd-note'), 'Add Note' quick-action @7276 focuses the composer; onAddNote/addNote @7217/8324/8861/20777 | ⚠️ partial | Functional in-session: submit calls onAddNote -> addNote pushes mkAct('note',{ts:0,...}) into page-local `extra` state (@7216) and it appears at the top of the timeline as 'Just now'. BUT it is EPHEMERAL — `extra` is component-local useState, never written to any store; navigating away and back (or refresh) loses the note. No persisted Note entity. Note is also not editable/deletable once added. |
| Log a CALL against the record | 'Make call' dialer popover @7276/7283 (onPopoverCall@7221), 'Log manually' LogCallModal @7280/7328 (onLogCall@7220) | ⚠️ partial | Contact-only. Logging a call pushes mkAct('call',{ts:0}) into `extra` and shows in the timeline with a 'Call logged' toast — but same ephemerality as notes (page-local `extra`, lost on nav). Deal/Company/Lead/Custom timelines do NOT pass onLogCall, so there is no 'log call' entry point on those histories (only onAddNote + onSchedule). |
| Add a TASK against the record | RecordTasks@7107 'Add task' -> TaskFormModal@17571 (save@7116 -> CalendarContext.addItem); wired on all five pages @7319/8035/8392/8929/20803 | ✅ supported | This is the one path that TRULY persists: a task is a real calendar-store record (type==='task') in shared CalendarContext (@2967/17625), survives navigation and appears on the global TasksPage. Creating/completing a task also calls onLog which pushes an ephemeral mkAct('task') activity into the timeline `extra` for immediate visibility. |
| See the logged item in that record's history (timeline) | ActivityTimeline@7070, buildActivityFeed@6760, items merge @7074 ([...extra, ...base] sorted by ts) | ⚠️ partial | Session-added items (note/call/meeting/task-log) DO render immediately at the top ('Just now', ts:0 sorts first). But the timeline itself is a render-time MOCK: base feed is regenerated by buildActivityFeed/mkAct on every mount and never stored (@6760, @6645). Real persisted Tasks are NOT pulled into the timeline base feed — the timeline shows a FAKE demo 'task' (e.g. 'Prepare proposal draft' @6702) unrelated to the RecordTasks rows. So 'history' and the real Task list are two disconnected surfaces. |
| Re-visit the record later and still see the logged note/call | extra state @7216/7986/8323/8860/20770 (useState, no context/store write) | ❌ broken | Notes, logged calls, and scheduled-meeting activities live only in page-local `extra`. There is no setContacts/updateContact/context write for activities — leaving the page or refreshing discards every note and logged call. Only Tasks persist. The 'record history' is therefore not durable for the note/call/activity portion of this use case. |


---

## 10. See all deals for a given Contact and for a given Company — ⚠️ partial

| Step | Screens touched | Status | Note |
|---|---|---|---|
| COMPANY SIDE — Open a Company detail page (CompanyDetailPage@8312, reached via CompanyRow/COMPANY_COLS goTo('company-detail',co.id)@8135/8146 which passes a real id). | Companies list → company-detail | ✅ supported | Navigation carries the company id; co=companies.find(x=>x.id===nav)@8316. Real record loads. |
| COMPANY SIDE — Read the 'Deals' panel on the company detail (@8384-8391). | company-detail Deals card | ✅ supported | Panel is driven by dl=dealsForCompany(co.name)@8319/8087, which filters allDeals() by d.company===name. allDeals()@8085 reads _liveDeals@8083 = live React deals state (_liveDeals=deals@21153), so it reflects runtime edits. Header shows real 'dl.list.length total · fmtK(dl.openValue) open'@8385. |
| COMPANY SIDE — Click a deal card to open the exact deal (@8388 goTo('deal-detail',d.id)). | company-detail → deal-detail | ✅ supported | Each card passes the real d.id; empty state 'No deals for this company yet.'@8387 renders correctly for companies with no name-matching deal. |
| COMPANY SIDE — Coverage / integrity of the company→deals join. | company-detail Deals card | ⚠️ partial | Join is by NAME STRING (d.company===co.name), not id. 8 of 17 seeded deals name companies that don't exist in COMPANIES_DATA (Northpeak Co, Pivot Studio, Barrett Digital, Apex Labs, Bluewave, Cedar Systems, Quill Media, Vertex Group @7372-7393) — those deals are unreachable from any company page. Duplicate-name companies (Meridian Agency@id1 vs Meridian Agency Inc.@id101; Summit Digital@id2 vs Summit Digital LLC@id102) split/mis-attach: only the exact spelling gets the deals. Also dealsForCompany counts open as stageKey!=='won'@8087, so LOST deals inflate the 'open' value/count. Renaming a company (updateCompany@21215/8332) does not cascade to deal.company, silently orphaning its deals. |
| CONTACT SIDE — Open a Contact detail page (ContactDetailPage@7193, reached via CONTACT_COLS/RecordSwitcher with a real id; c=contacts.find(x=>x.id===nav)@7197). | Contacts list → contact-detail | ✅ supported | Real contact record loads by id. |
| CONTACT SIDE — Read the 'Deals · 2' panel on the contact detail (@7302-7305) to see this contact's deals. | contact-detail Deals card | ❌ broken | The panel is FULLY HARDCODED: the count '· 2' and the two cards (Q3 Engagement/$24k/Proposal, Annual Retainer/$48k/Qualified) are literals @7304, identical for every contact and disconnected from any real deal data. There is no dealsForContact() helper. Each card's onClick is goTo('deal-detail') with NO id@7304, so it opens an arbitrary default deal (deals.find fallback), not this contact's deal. The 'Recommended next action' card @7313-7314 likewise hardcodes 'the $24k deal'. |
| CONTACT SIDE — Fallback: find this contact's real deals via the Activity timeline (ActivityTimeline kind='contact'@7320 → buildActivityFeed@6767). | contact-detail Activity timeline | ⚠️ partial | buildActivityFeed DOES derive real related deals by name string (d.primaryContact===rec.name OR d.company===rec.co)@6767 and emits deal-tagged activity chips that navigate to the real deal id@6768. But this is a merged activity feed, not a deals LIST — deals surface only as scattered per-deal events, there is no count or roster, and it disagrees with the fake 'Deals · 2' panel above it. Join is name-string only, so a renamed/duplicate contact or company detaches it. |
| CONTACT SIDE — Alternative: jump from the contact to its company to view deals there (the contact's 'Company' field 'Open company' button@7233). | contact-detail Company field → company-detail | ❌ broken | The companylookup field passes go:(g)=>g('company-detail')@7233 with NO id; PCompanyLookup onOpen@6205 → case 'companylookup'@6489 calls f.go(goTo) → goTo('company-detail') with no argument. company-detail then falls back to companies[0]@8316 — it opens a DEFAULT company, not this contact's company. So the cross-navigation that would let you reach the (working) company Deals panel is broken. |


---

## 11. Filter and search each entity list (leads, contacts, companies, deals) — ⚠️ partial

| Step | Screens touched | Status | Note |
|---|---|---|---|
| Open the Contacts list and type in its search box (name / company / email) | ContactsPage @6263; search input @6350 (value=search state @6266); filter applied @6310 (c.name/c.co/c.email .includes(q)) | ✅ supported | Dedicated 'Search contacts...' box. Case-insensitive substring across name, co (company name string), email. Search chip + Clear-all @6362-6363. |
| Filter the Contacts list by field conditions (status, owner, score, tags, dates, etc.) | OmFiltersButton object='contact' @6352; field schema omFieldSchema('contact') @5574-5579; engine omApplyFilter @6308/5619; per-cond eval omEvalCond @5608 | ✅ supported | Full multi-condition AND/OR builder with text/number/select/owner/multiselect/date/boolean operators (OM_OPS @5532). Filter chip shows count @6360. |
| Sort the Contacts list by any column | OmSortHeader @6371 -> onSort @6300 -> omNextSort @5644; omSortRows @6311/5639 | ✅ supported | Click column header cycles asc/desc/off; sort chip @6361. |
| Open the Companies list and search it (name / website / industry) | CompaniesPage @8176; search input @8262; filter @8222 (co.name/co.domain/co.industry .includes(q)) | ✅ supported | Same pattern as Contacts; dedicated 'Search companies...' box. |
| Filter and sort the Companies list | OmFiltersButton object='company' @8263; omFieldSchema('company') @5580-5584; omApplyFilter @8220; OmSortHeader @8279 -> omSortRows @8223 | ✅ supported | Company filter fields include computed 'Open deals' (dealsForCompany) which double-counts lost deals as open (dealsForCompany @8087 uses stageKey!=='won'), so a numeric filter on Open deals is inflated — but the control itself works. |
| Open the Leads list and search it (name / company / email) | LeadsPage @8706; search input @8795; filter @8755 (l.name/l.company/l.email .includes(q)) | ✅ supported | Dedicated 'Search leads...' box. Note leads use l.company (free-text name string) since a Lead has no companyId. |
| Filter and sort the Leads list | OmFiltersButton object='lead' @8796; omFieldSchema('lead') @5585-5591; omApplyFilter @8753; OmSortHeader @8818 -> omSortRows @8756 | ⚠️ partial | Works on the default/custom views. But on the 'Converted' and 'Archived' system views the entire Filters+Sort chrome is suppressed (special flag @8728; !special guards @8752-8756, @8796) — search still runs but you cannot filter/sort converted or archived leads. The lead Status filter also exposes 'Qualified' (LEAD_STATUSES @8437), the fused lifecycle-vs-deal-pipeline value. |
| Open the Deals list and search it by text (deal/company/contact name) | PipelinePage @7678; List view render @7886-7900; there is NO search <input> anywhere in PipelinePage (compare the 'Search X...' boxes at 6350/8262/8795 which have no deal equivalent) | ❌ broken | Deals have no list-level text search box. Deals are only text-searchable through the global command palette (@1647), which matches only company+value, caps at 4 results, and navigates to deal-detail rather than filtering the list. So the 'search the deals list' step has no screen. |
| Filter the Deals list by field conditions | OmFiltersButton object='deal' @7838; omFieldSchema('deal') @5592-5597; dealsFor/omApplyFilter @7767; list view honors workFilter @7784/7799 | ⚠️ partial | Multi-condition filter works, BUT the Stage select field's options are hardcoded ['Prospecting','Qualified','Proposal','Negotiation','Won'] @5594 and OMIT 'Lost'. Because select filters only allow picking from that option list (OmMultiPicker, ops 'is any of'/'is none of' @5537), you can never filter deals to (or exclude) the Lost stage even though lost deals exist (stageKey 'lost' @7393). 'Qualified' here is the same fused value as the Lead lifecycle. |
| Sort the Deals list | OmSortHeader @7894/7896 -> onSort @7797; omSortRows @7800; default sort closeDate asc | ✅ supported | List view sort works across the flat deal rows. |


---

## 12. Reassign ownership of a record (change the Owner of a Contact / Lead / Company / Deal / Custom record) — ⚠️ partial

| Step | Screens touched | Status | Note |
|---|---|---|---|
| Open the object list (Contacts/Leads/Companies/Deals) and click the record's Owner avatar in the row to open the 'Reassign to' dropdown, then pick a team member | ContactOwnerDropdown @4950-4956; owner column cells CONTACT_COLS @5512 / DEAL_COLS @7509 / company @8151 / lead @8682 | ✅ supported | Inline avatar dropdown lists TEAM_MEMBERS and calls onOwner. Reducers setOwner (contacts @6293, companies @8205, leads @8737) and lvOwner (deals @7806) write BOTH owner:m.avatar and ownerColor:m.color to real React state, then toast 'Reassigned to '+m.name. Correctly gated by effective role via disabled={x.readOnly} (readOnly:!_cEdit). This is the primary, working path. |
| Select multiple records with the checkboxes and use the bulk action bar 'Assign owner' menu to reassign them all at once | BulkBar 'Assign owner' button + panel @6239-6240; assignOwner @6217; patchSel @6215 | ✅ supported | assignOwner patches {owner:m.avatar,ownerColor:m.color} onto every selected id via setRows, toasts 'Owner set to X on N contacts'. Gated by canEdit=effCanObject(OBJ,'edit') @6233. Works for contact/company/lead/deal lists. |
| Open a standard record's detail page and change the Owner inline in the properties panel | POwner @6445-6448; contact owner field cPrimary @7233 -> onFieldChange @7232 -> updateContact @21213; deal owner field dealPrimary @7992 -> onDealField @7991 -> updateDeal @21216 | ⚠️ partial | POwner commits ONLY the avatar code: updateContact(id,{owner:avatar}) / updateDeal(id,{owner:avatar}). It never writes ownerColor. POwner's own display re-derives color via userByCode(value) @6446 so the detail page looks right, BUT the list/board/kanban read the stale c.ownerColor directly (row cells @5512/7509, board col @20685). Result: after a detail-page reassign, the record's avatar shows the NEW initials on the OLD owner's color everywhere the color field is read. Also: editing owner via the record's Edit modal (onEditSave @7239 / onDealField edit) has the same avatar-only patch. |
| Open a custom-object record's detail page and change its Owner inline | CustomRecordDetailPage @20762; CoProperties @20629-20643 (renders only def.fields via CoFieldDisplay) | ❌ broken | There is NO owner editor on the custom-record detail page — CoProperties only renders user-defined fields; owner is not among them and CoLinksPanel only shows relationships. Owner can be changed on a custom record ONLY via the Edit drawer (CustomRecordDrawer owner <select> @20605, which correctly sets ownerColor:coOwnerColor @20598) or by dragging on the board. Inline detail-page reassignment (available for every standard object) is missing for custom objects. |
| See the reassignment reflected as an activity / audit entry on the record's timeline | actContactOwn 'Owner changed' mkAct @6705; buildActivityFeed @6760; ActivityTimeline @7070 | ❌ broken | No reassignment path emits an activity. The only 'Owner changed' timeline entry (@6705) is a hardcoded demo activity regenerated every render with static payload {from:'Unassigned',to:A} — it shows the SAME 'Reassigned for territory coverage' line on every contact regardless of the real owner or whether anyone ever reassigned. Owner reassignment produces a transient toast only; the change is not journaled and is lost as history. |


---

## 13. Reporting: pipeline value by stage, lead→deal conversion rate, win/loss, activity volume — traced through the Reports page (ReportsPage@11723) which reuses the dashboard widget engine (WIDGET_DEFS@3792, renderWidgetBody@4537). — ⚠️ partial

| Step | Screens touched | Status | Note |
|---|---|---|---|
| Open the Reports screen | NAV item 'reports'@2460 / ReportsPage@11723; role-preset layouts REPORT_ROLE_PRESETS@11706; grid of DashboardWidget@11794-11802 | ✅ supported | Reports is a real, role-scoped, customizable page (add/remove/resize/reorder widgets, per-role & workspace presets). reportData is scope-filtered by Own/Team/All@11750. This part is solidly supported. |
| Pipeline value by stage | 'deals-by-stage' widget (WIDGET_DEFS@3797, bars renderer@4569-4576, DashWidgetChart@4244) fed by dashStages()@3789 / allDeals()@8085; headline 'pipeline-value' stat via dashStatLive@4520 | ✅ supported | The per-stage bars/donut and the Pipeline Value KPI compute LIVE from flattened deals (sum of open deal.amount, grouped by stageKey→stage name). Genuinely live. Caveat: the bars widget header total is HARDCODED '$382k · ${range}'@4570 (real bars, fake summary line), and every stat card's trend badge (+12% etc.) is a frozen def.d constant@3793, not computed. |
| Win / loss (win rate) | 'win-rate' widget stat card, computed in dashStatLive@4522 (won.length/(won+lost)); win/loss derived from deal.stageKey==='won'/'lost' | ✅ supported | Win rate is computed live from deals. Works because Won/Lost are terminal stages (STAGES_DATA@7370). Limits: it counts ALL-time won vs lost with no date-range applied; loss REASONS captured by DealOutcomeModal@7465 are never persisted (only toasted@7927), so a loss-reason report is impossible. The -2% delta on the card is a frozen constant, not real. |
| Lead → deal conversion rate | 'lead-conversion' metric@4562 and 'conversion-funnel' widget (DashConversionFunnel@4431) | ❌ broken | NOT a lead→deal conversion rate. lead-conversion computes qualified-STATUS leads / total active leads (l.status==='Qualified')@4562 — a snapshot of lead lifecycle status, not a conversion outcome. conversion-funnel@4431 stacks three UNRELATED numbers: leads.length, count of status==='Qualified' leads, and allDeals won count@4434 — with no join between a lead and the deal it converted into. Worse, dashObjRows('lead')@4088 filters OUT l.converted leads, so leads that actually converted are excluded from the numerator entirely. It measures the fused 'Qualified' concept, not real Lead→Deal conversion. No screen reports the buildConvertRecords@8556 convertedToDealId lineage. |
| Activity volume | 'calls-logged'@3805 & 'emails-sent'@3806 stat cards; 'call-stats' DashCallStats@4390; 'activity-feed' DashActivityFeed@4347; 'engagement-trend' DashEngagement@4420; 'recent-activity'/'team-activity' (DASH_ACT@3790) | ⚠️ partial | Mixed. call-stats IS live from CALLS_DATA@15590. But the two headline volume KPIs calls-logged('34') and emails-sent('184') are FROZEN def.d constants — dashStatLive@4515 only handles deal metrics, so activity stats never compute and object:'activity'@4005 has no data source. engagement-trend is SYNTHETIC (Math.sin-generated series@4424). recent-activity/team-activity render the static DASH_ACT array@3790. Activity 'volume' is largely decorative; only call-stats reflects data. |
| Apply a reporting date range | Range selector in Reports topbar@11743-11787 (RANGES 'Last 7/30/90 days'…) | ❌ broken | The range dropdown sets local state 'range'@11743 that is only interpolated into widget subtitles (e.g. `$382k · ${range}`@4570). It is NOT passed into reportData or any widget's data query — dashStatLive/dashStages/win-rate ignore it. Every metric is all-time regardless of the selected range. Purely cosmetic. |
| Export the report (CSV) | Export CSV button@11788 (gated by effCanExport()) | ❌ broken | Toast-only. onClick just shows a 'CSV downloaded' toast@11790 for 3s and generates no file. No export of pipeline/conversion/win-loss/activity data actually occurs. |


---

## 14. Handle a lost / disqualified lead without losing the record — ⚠️ partial

| Step | Screens touched | Status | Note |
|---|---|---|---|
| Open the lead from the Leads list and land on its detail page | LeadsPage list (LeadsPage@8706, LeadRow@8538) -> LeadDetailPage@8853; row/name click goTo('lead-detail',l.id)@8542; detail resolves l=leads.find(x=>x.id===nav)@8857 | ✅ supported | Real, wired against live React state. The lead opens on a real detail page with its status dropdown, score, activity timeline and record properties. |
| Mark the lead as lost/unqualified via its lifecycle status | LeadStatusDropdown@8536 (ChipSelect@8089) on the detail header@8909 and on each list row@8545; writes updateLead(l.id,{status:s}); LEAD_STATUSES includes 'Unqualified'@8437 | ⚠️ partial | Setting status to 'Unqualified' is a REAL state write (updateLead / setStatus@8736) and persists. BUT it is a bare value change: ChipSelect only swaps the string (@8089-8091) — no reason, no note, no confirmation is captured. The lifecycle is correctly a status (no pipeline on the Lead), which is good, but 'Qualified' is shared with the Deal pipeline stage (DEAL_STAGE_ORDER@7969), the classic fused-concept naming. |
| Record WHY the lead was lost/disqualified (loss reason) | Nowhere. LeadDetailPage@8853 has no loss-reason field; leadPrimary/leadSecondary schemas@8868-8869 have no reason field; the 'More actions' menu@8895-8898 offers only 'Edit lead' and 'Delete lead' — no 'Disqualify' action. QualBadge/QualificationCard (@17834/17841) render 'Review' only when a pre-seeded qual object with status:'disqualified' exists (set upstream by booking@18337, never by a user action here). | ❌ broken | There is NO screen or field to capture a disqualification/loss reason on a lead. The Deal side has a DealOutcomeModal that at least collects a reason (@7465-7487) — though it too only toasts it — but the Lead side has nothing comparable. The reason for losing the lead is simply unrecordable; the record loses the 'why'. |
| Archive the lead so it leaves active lists but the record is kept | Per-row Archive button (LeadRow@8549 onArchive->doArchive@8738) and BulkBar Archive@6256 set {archived:true} in real state; nonArch filter@8746 hides archived leads from the working list. | ⚠️ partial | Archive works and persists (real setRows write with a confirm dialog). The gap: the archive action does NOT exist on the LeadDetailPage 'More actions' menu (@8895-8898 only has Edit/Delete). A user who followed the natural path (open the lead, change status to Unqualified) cannot archive from where they are standing — they must go back to the list row or select it in bulk. So the record IS kept, but the archive entry point is inconsistent with the detail flow. |
| Find and reopen the lost lead later | Leads left rail 'Archived' view (bottomViews@8791, countFor archived@8747, base filter archived&&!converted@8751); Restore button on archived rows (LeadRow@8549 onRestore->doRestore@8739 clears archived:false). | ✅ supported | The Archived view lists archived leads and the per-row Restore returns them to active lists via real state. Nothing is lost. Note there is no dedicated 'Unqualified' or 'Lost' saved view (LEAD_DEFAULT_VIEWS@8526-8535 has New/Contacted/Qualified/Hot but no Unqualified), so a status-only disqualification without archiving just sits in 'All Leads' unless the user builds a custom filter. |
| Alternative disposition: soft-delete the lead to the Recycle Bin and restore within 30 days | 'Delete lead' in detail More-menu@8897 -> doDelete@8879 sets {deleted:true,deletedAt,deletedBy} (also bulk delete@6258); SettingsRecycleBinPage@12024 lists deleted leads (rows build@12052) with real Restore (@12053) and purge (@12054). | ⚠️ partial | Soft-delete + Recycle Bin restore is fully real and preserves the record for 30 days. Caveat: the Recycle Bin is admin-gated — settingsIsAdmin()@12033 blocks non-admins entirely (@12034-12040), so a Sales Rep who deletes a lead cannot restore it themselves. deletedBy is hardcoded 'Alex Morgan'@8879 regardless of the acting user. |

