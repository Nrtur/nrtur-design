# Re-Audit — after Waves 1-3
_Generated by crm-gap-audit (Phase 6 loop) · 2026-07-01 · verified against index.html_

**Scorecard:** 25 Criticals verified CLOSED · 1 new defect · use cases: 0 broken / 1 partial / rest OK · 15 remaining gaps.

> **Bottom line:** The vocabulary, seed migration, join helpers, rename cascade, and Convert flow are solid. But the id-first guarantee stops at the seed/Convert boundary (R1), multi-pipeline is cosmetic (R2), and activities are throwaway (R3). TOP 3 NEXT: (1) close R1/B1 by resolving picked names→ids and stamping companyId/primaryContactId at every live create site (drawer, funnel, booking, ad, bulk-convert) — this also fixes the duplicate-name mis-open; (2) scope the pipeline board by deal.pipeline (R2, one-line filter change); (3) persist activities/notes into shared context keyed by record id (R3) so logging and activity reporting are real. R4 (capture→Lead) and R6 (User entity) are the next tier.


## Regressions / new defects (act on these)

### (B1) NEW DEFECT — Contact-page 'New deal' supplies the correct ids but the create path throws them away; deal is born name-only and can open the WRONG duplicate-named person
`NEW-DEFECT` · **High** · prefill emitted ContactDetail :7316; buildInitial :2345; arBuildRecord deal branch :2325; resolved back DealDetailPage :7998

CONCRETE FAILURE: two seed contacts are both 'Sarah Chen' at Meridian — id:1 (line 4926) and id:101 (line 4936). Open id:101's page, click 'New deal' (prefill correctly carries contactId:101). The saved deal has primaryContact:'Sarah Chen' with no id, so DealDetailPage name-matches the FIRST 'Sarah Chen' = id:1 — the wrong person. Partially re-introduces the exact 'deal opens wrong person' defect Wave 3 claimed to close, for newly-created deals under duplicate names. Distinct from the deferred ARLookup item because the correct id is already supplied by the caller and discarded. FIX (small): have buildInitial/arBuildRecord honor prefill.companyId/contactId and stamp companyId/primaryContactId. Non-duplicate names still link via fallback, so severity High not Critical.

_Evidence:_ 7316 `window.__nrturQuickAdd('deal','single',{contact:c.name,contactId:c.id,company:c.co,companyId:c.companyId})` — ids ARE passed. But 2345 buildInitial only reads names: `if(prefill.company&&f.type==='Lookup'...)o[f.id]=prefill.company;if(prefill.contact&&...)o[f.id]=prefill.contact;` — never reads prefill.companyId/contactId. 2325 arBuildRecord deal returns `company:company||name,...primaryContact:g(/contact/)` with NO companyId/primaryContactId keys. So the ids are dropped and 7998 dealContact name-matches: `(d.primaryContact?crm.contacts.find(c=>c.name===d.primaryContact&&!c.deleted):null)`.

### (B2) NO other regression found in the CORE model — waves did not break existing surfaces
`CLOSED` · **High** · GlobalSearch 1644-1647; CMD_ITEMS; DashConversionFunnel 4446/4449; dashStages 3800; win-rate 4533; Leads board 8828; auto-created companies vs CompaniesPage 8237/COMPANY_COLS 8164-8175

Aside from B1, the three waves introduced no regression on the Company/Contact/Deal/Lead/pipeline core.

_Evidence:_ Search consumes only display-name fields (c.co/domain/company) that the migration ADDED alongside, never removed (1644-1647). Dashboards use Sales-Ready predicates + stable stageKey (4446,4533,3800). Leads board writes the exact target-column status via setStatus (8828). Auto-created companies get type:'Prospect'+industry+avatar with no `deleted` flag, so they pass the CompaniesPage filter and COMPANY_COLS renderers guard every optional field ('—' fallbacks). No stale-constant CORE join and no label/predicate mismatch found.

### (E) BOTTOM LINE — core model is now substantially correct & internally consistent for SEED and CONVERT data; the remaining risk is that live-created records and multi-pipeline/activity behavior are not yet real
`PARTIAL` · **Critical** · summary across A1-A9, B1, R1-R6

The vocabulary, seed migration, join helpers, rename cascade, and Convert flow are solid. But the id-first guarantee stops at the seed/Convert boundary (R1), multi-pipeline is cosmetic (R2), and activities are throwaway (R3). TOP 3 NEXT: (1) close R1/B1 by resolving picked names→ids and stamping companyId/primaryContactId at every live create site (drawer, funnel, booking, ad, bulk-convert) — this also fixes the duplicate-name mis-open; (2) scope the pipeline board by deal.pipeline (R2, one-line filter change); (3) persist activities/notes into shared context keyed by record id (R3) so logging and activity reporting are real. R4 (capture→Lead) and R6 (User entity) are the next tier.

_Evidence:_ All four Wave 1 stage-canonicalization claims (A1-A4), all Wave 2 lead/deal-separation claims (A5), and all six Wave 3 id-linking claims (A6-A9) are VERIFIED CLOSED against current code. The waves introduced exactly ONE new defect (B1) and no other core regression (B2).


## Criticals verified CLOSED

### (A1) ONE canonical deal-stage source, derived from STAGES_DATA — CLOSED
`CLOSED` · **Critical** · index.html:7984-7987 (DEAL_STAGES_ALL/ORDER/COLOR/KEY_BY_NAME); STAGES_DATA seed :7383-7406

Anchor is 7984-7987, not the '~7969' in the change brief (file shifted); functionally correct.

_Evidence:_ 7984 `const DEAL_STAGES_ALL=STAGES_DATA.map(s=>s.name);` / 7985 `DEAL_STAGE_ORDER=STAGES_DATA.filter(s=>s.key!=='lost').map(s=>s.name);` / 7986 `DEAL_STAGE_COLOR=Object.fromEntries(STAGES_DATA.map(s=>[s.name,s.color]));` / 7987 `DEAL_STAGE_KEY_BY_NAME=Object.fromEntries(STAGES_DATA.map(s=>[s.name,s.key]));`. All four derive from the single STAGES_DATA seed, so name/key/color cannot drift.

### (A2) Deal DETAIL reads canonical stage source (ladder/select/pill) — CLOSED
`CLOSED` · **Critical** · DealDetailPage index.html:7992,8007,8009,8047,8085

d.stageName comes from canonical STAGE_BY_KEY; a Prospecting deal reads 'Prospecting' (no more `||'Qualified'` fallback in this path).

_Evidence:_ 7992 `const [stage,setStage]=React.useState(d.stageName);` 8007 `stageIdx=DEAL_STAGE_ORDER.indexOf(stage);nextStage=DEAL_STAGE_ORDER[stageIdx+1];color=DEAL_STAGE_COLOR[stage];` 8009 write-back `stageKey:DEAL_STAGE_KEY_BY_NAME[v]`. Verified no divergent hardcoded stage list on the detail page.

### (A3) New deals default to Prospecting in both quick-add and CSV import — CLOSED
`CLOSED` · **Critical** · arBuildRecord index.html:2325; arBuildFromSheet index.html:2332

Verified verbatim in current code.

_Evidence:_ 2325 `const stageName=g(/stage/)||'Prospecting';const keyMap={Prospecting:'prospecting',...};const sk=keyMap[stageName]||'prospecting';`. 2332 `const stageName=row.stage||'Prospecting';...stageKey:keyMap[stageName]||'prospecting'`. Old 'Qualified' default is gone from both.

### (A4) Won/Lost outcome+reason persisted on deal.outcome via DealOutcomeModal (board-drag path) — CLOSED
`CLOSED` · **Critical** · moveDeal index.html:7779; onConfirm :7940; DealOutcomeModal :7478-7500; display :8047

Persisted ONLY on the board-drag/list-move path; closing from the detail ladder/pill still writes no reason (see remaining-gap R-D3). That's a documented deferral, not a new break.

_Evidence:_ 7779 `const kind=/lost/.test(nm)?'lost':/won|live/.test(nm)?'won':null;if(kind){setOutcomePrompt({dd,toStage,kind});return;}`. 7940 onConfirm persists `{...x,outcome:{result:kind,reason,note:note||'',closedAt:Date.now(),closedBy:'Alex Morgan'}}`. Displayed on detail at 8047.

### (A5) Leads fully renamed to New/Contacted/Nurturing/Sales-Ready/Disqualified everywhere; DEAL 'Qualified' + PILL_C key intentionally kept — CLOSED
`CLOSED` · **Critical** · LEAD_STATUSES :8456; chip/dot :8457-8458; STAGES_DATA :7389; PILL_C :6418; dashboards 4446/4449/4573/4575; booking 18376/18381

PILL_C ADDED Sales-Ready/Disqualified rather than renaming, so deal + contact pills still color.

_Evidence:_ 8456 `LEAD_STATUSES=['New','Contacted','Nurturing','Sales-Ready','Disqualified']`. Every lead label is backed by a matching Sales-Ready predicate (funnel 4446/4449, source filter 4079/4112, saved view 8550). The only surviving 'Qualified'/'Unqualified' strings are the DEAL stage (STAGES_DATA 7389), Contact c_status (2157,14043), one Contact seed (4933), and PILL_C keys (6418) — all deliberately kept. Zero lead-object comparisons still test 'Qualified'/'Unqualified'.

### (A6) migrateLinks auto-creates missing companies (deterministic ids) & backfills all id links at App init — CLOSED
`CLOSED` · **Critical** · migrateLinks index.html:21164-21195; invoked :21197

Pure and safe to re-run; result seeds state once via useState initializers (21202/21204/21209).

_Evidence:_ 21171 `let nextId=Math.max(0,...companies.map(c=>c.id||0));` 21176-21178 `nextId+=1;const rec=nrturNewCompany(name);rec.id=nextId;companies.push(rec);` — deterministic contiguous ids on a LOCAL copy (21165 `companiesSeed.map(c=>({...c}))`), never mutating COMPANIES_DATA. 21188-21192 backfill contact.companyId, deal.companyId/primaryContactId/additionalContactIds. Idempotent via `if(...==null)` guards.

### (A7) Core joins are id-first with name fallback and read LIVE state — CLOSED
`CLOSED` · **Critical** · contactsForCompany :8105; dealsForCompany :8106; _liveContacts mirror :21211; DealDetailPage :7997-7998; Contact myDeals :7210; CompanyDetail :8337-8338

The old `contactsForCompany(deal.company)[0]` shortcut is now only the final fallback; real primaryContact resolves correctly.

_Evidence:_ 8105 `const src=_liveContacts||CONTACTS_DATA;return src.filter(c=>!c.deleted&&((id!=null&&c.companyId===id)||((c.companyId==null||id==null)&&c.co===name)));`. 7997 companyRec id-first then `crm.companies.find(c=>c.name===d.company)`. 7998 dealContact resolves primaryContactId FIRST, then name, then first-at-company only as last resort. 7210 myDeals matches primaryContactId/additionalContactIds/companyId before name. Live mirrors set each render (21210-21211).

### (A8) updateCompany/updateContact rename cascade prevents orphans & upgrades legacy name-links to id-links — CLOSED
`CLOSED` · **Critical** · updateCompany :21273; updateContact :21271

A rename can no longer orphan children and actually upgrades name-only children to stable id links.

_Evidence:_ 21273 on rename `setContacts(cs=>cs.map(ct=>((ct.companyId===id)||(ct.companyId==null&&ct.co===oldName))?{...ct,companyId:id,co:newName}:ct));` and the same for setDeals — cascades to id-linked children AND back-fills companyId onto legacy name-linked children. 21271 updateContact refreshes deal.primaryContact where primaryContactId===id.

### (A9) buildConvertRecords mints id-linked Contact+Company+Deal (null companyId when no company) — CLOSED
`CLOSED` · **Critical** · buildConvertRecords :8575-8588; doConvert :8902

Convert flow is the ONE UI create path that is fully id-linked from birth (contrast R-1).

_Evidence:_ 8577 `const contactId=base+1,companyId=base+2,dealId=base+3;`. 8582 contact `companyId:(payload.company?companyId:null)`. 8586 deal `companyId:(payload.company?companyId:null),primaryContactId:contactId,additionalContactIds:[],stageKey:'prospecting',dStage:'Prospecting'`. doConvert 8902 persists all three and back-links the lead.


## Use-case matrix

### (C-UC1) Capture a lead → gets a canonical lifecycle status
`supported` · **High** · arBuildRecord :2326; arBuildFromSheet :2333; booking :18381; automation createLead :10813

Caveat: this holds ONLY for the manual/booking/automation paths. Funnels, forms and ad platforms bypass it (see R-4).

_Evidence:_ 2326 `let status=g(/^status$/)||'New';if(!LEAD_STATUSES.includes(status))status='New';`. 2333 same guard. Booking 18381 writes Sales-Ready/Disqualified/New. Every manual capture path assigns a valid status and defaults unknowns to New.

### (C-UC2) Qualify + convert a lead → id-linked Contact+Company+Deal
`supported` · **High** · LeadStatusDropdown :8931; board setStatus :8757/8828; buildConvertRecords :8575; doConvert :8902

Single-lead convert is fully correct; the BULK convert is fake (see R-5).

_Evidence:_ 8931 status advance persists via updateLead. 8577-8586 mint id-cross-linked records; 8902 persists all three and back-links the lead (convertedTo* ids). Company-less convert leaves both companyId null.

### (C-UC3) New deal enters pipeline at Prospecting
`supported` · **High** · arBuildRecord :2325; arBuildFromSheet :2332; buildConvertRecords :8586

Caveat: the quick-add drawer's Stage dropdown cannot explicitly PICK Prospecting (create field options omit it — see R-6); it only defaults there when left unset.

_Evidence:_ All three default the first stage to Prospecting; keyMap falls back to 'prospecting' for unknown stages.

### (C-UC4) Move a deal through stages
`supported` · **High** · onDealField :8009; ladder :8047; board applyMove :7778

Fully consistent.

_Evidence:_ Detail pill options=DEAL_STAGES_ALL (all 6 incl. Lost); ladder uses DEAL_STAGE_ORDER; board applyMove sets `stageKey:toStage.key`. Vocabulary single-sourced so detail/board/list agree.

### (C-UC5) Close Won/Lost with a persisted reason
`partial` · **High** · board-drag onConfirm :7940 (persists); detail ladder :8047 + pill :8009 (do NOT)

Reason captured ONLY on board-drag. Explicitly deferred (pipeline-stage-correctness.md line 40). PARTIAL, not a new defect.

_Evidence:_ 7940 board-drag writes deal.outcome{result,reason,note,closedAt,closedBy}. But 8047 ladder Won/Lost only runs `commit=()=>{setStage(s);if(k)updateDeal(d.id,{stageKey:k});}` with no reason, and 8009 pill sets stageKey directly with no confirm/reason.

### (C-UC6) Post-sale: records persist & converted lead locked read-only
`supported` · **High** · doConvert :8902; canEditLead :8889 + gates 8914/8918/8931/8936/8952

All five gates present and wired to !l.converted.

_Evidence:_ 8889 `canEditLead=effCanObject('Leads','edit')&&!l.converted` gates Schedule/Edit/status-dropdown/properties(readOnly)/activity-composer. Minted deal carries primaryContactId+companyId so it appears on contact/company.

### (C-UC7) See ALL deals for a Contact and a Company (real id-joined panels)
`supported` · **High** · Contact myDeals :7210 + render :7317; dealsForCompany :8106; contactsForCompany :8105

Both id-first with name fallback, live-state backed.

_Evidence:_ 7210 id-first filter (primaryContactId/additionalContactIds/companyId then name); 7317 renders live deals with goTo('deal-detail',d.id) — no hardcoded fake deals. Company panels id-join via 8106/8105 on live state.

### (C-UC8) Rename a company without orphaning children
`supported` · **High** · updateCompany cascade :21273; updateContact :21271

Cosmetic residual: deal.additionalContacts[].contact display string not refreshed on contact rename, but additionalContactIds keep the join intact.

_Evidence:_ Rename cascades to id-linked AND legacy name-linked children and back-fills companyId; contact rename refreshes deal.primaryContact by primaryContactId.

### (C-UC9) Reporting: pipeline-by-stage, lead→deal conversion, win/loss
`supported` · **High** · dashStages :3800; win-rate :4533; DashConversionFunnel :4442-4450; lead-funnel :4575

Predicates use the new vocab + stable stageKey; company rollups id-safe. Caveat: reporting on 'activity' is fiction because notes/calls aren't persisted (R-3).

_Evidence:_ 3800 pipeline live per canonical stageKey; 4533 win-rate live-computed from won/lost stageKeys; funnel uses `l.status==='Sales-Ready'` and iterates LEAD_STATUSES. Headline KPI cards render live values overriding frozen def constants.


## Remaining gaps (prioritized)

### (D-R1) CRITICAL — Records created through the live UI are NOT id-linked; only the one-time seed migration and Convert flow set companyId/primaryContactId
`OPEN` · **Critical** · arBuildRecord :2324-2327; buildInitial :2345; funnel deal :19877; ad capture :21250; booking deal :18392; migrateLinks runs once :21197

The id-first model degrades to name-fallback for everything a user actually adds after load. Not a hard break (contactsForCompany/dealsForCompany/myDeals all keep a name fallback, and updateCompany rename back-fills companyId on the next rename), but Wave 3's 'new records link by stable id from birth' does NOT hold for the quick-add/funnel/booking/ad paths. B1 is the acute sub-case (ids supplied then dropped → wrong duplicate). FIX (medium): resolve picked company/contact names to ids at each commit site and stamp companyId/primaryContactId.

_Evidence:_ arBuildRecord deal (2325) sets `company:company||name`/`primaryContact:g(/contact/)` with no ids; contact (2327) sets `co` with no companyId. buildInitial (2345) maps only prefill NAMES. Funnel deal (19877) sets `primaryContact:contactName` but no ids. Ad capture (21250) creates a Contact with no companyId. migrateLinks (21197) runs once on the seed only — never backfills runtime-created records.

### (D-R2) CRITICAL — deal.pipeline does not scope the board; every deal shows on every pipeline (multi-pipeline is cosmetic)
`OPEN` · **Critical** · PipelinePage.dealsFor :7780; pipelines seed :7727-7735

The 2nd built-in ('Customer Onboarding') uses demo cards so it hides the leak today, but any user-created real pipeline or an Onboarding-tagged deal with a real stageKey exposes it. FIX (small-medium): add `&&(d.pipeline||'Sales Pipeline')===active.name` to the real-stage filter and seed user pipelines with real stageKeys.

_Evidence:_ 7780 `const dealsFor=(stage)=>{let arr=stage.real?_deals.filter(d=>d.stageKey===stage.key&&!d.deleted):(stage.demo||[]);...}` — filters ONLY by stageKey, never d.pipeline. The 'Sales Pipeline' real stages therefore render ALL deals regardless of deal.pipeline. deal.pipeline is written by every create path (2137,4078,8010,8670,19877) but READ by nothing on the board.

### (D-R3) CRITICAL — Activities & notes are not persisted; feed is regenerated from the record each render and session-added notes live in component-local state lost on navigation
`OPEN` · **Critical** · ActivityTimeline base :7084; extra state ContactDetail :7229 / DealDetail :8004 / others 8342/8882/20799; buildActivityFeed :6771

'Log a note/call/meeting' is throwaway; any activity-based reporting is fiction. FIX (large): move notes/calls/meetings into shared CrmDataContext keyed by record id, like deals/contacts.

_Evidence:_ 7084 `const base=React.useMemo(()=>buildActivityFeed(kind,rec,ctx||{}),[...])` — deterministically rebuilt from the record every render (no stored events). 7229 `const [extra,setExtra]=React.useState([]); // session-added activities`; addNote/onLogCall/onSchedule all `setExtra(x=>[mkAct(...),...x])` (7230,7233,7236) — per-component-instance, init [] on every mount, never written back to record/context. Navigating away re-mounts and resets extra to [].

### (D-R4) HIGH — Capture channels (funnels, forms, ad platforms) create Contacts, never Leads, bypassing the lead lifecycle Wave 2 built
`OPEN` · **High** · funnel submit :19869; ad ingest :21250

The lead funnel/conversion dashboards only ever see manually-added leads. An automation `createLead` action exists (10813) but the capture code paths don't use it. FIX (medium): inbound capture should mint Leads (setLeads) that then flow through Convert.

_Evidence:_ 19869 funnel `crm.setContacts(a=>[{...cstatus:'Prospect',source:'Funnel'...}])` — direct Contact, no Lead. 21250 ad ingest `setContacts(a=>[{...status:'New',cstatus:'Prospect'...}])` then only a tracking row to setAdLeads (21251). Inbound demand skips New→Contacted→Nurturing→Sales-Ready entirely.

### (D-R5) HIGH — Bulk 'Convert' on Leads is a fake toast-only action; creates no Contact/Company/Deal
`OPEN` · **High** · Leads bulk action bar :6266

A multi-select 'convert' silently creates nothing. FIX (small-medium): loop selected leads through buildConvertRecords+persist, or open a batch convert modal.

_Evidence:_ 6266 `{canEdit&&object==='lead'&&<button title="Convert" onClick={()=>{close();showToast(n+' '+word+' queued for conversion');}}...>` — handler only closes the menu and toasts; never calls buildConvertRecords/doConvert, never persists, never marks leads converted. Contrast single-lead doConvert (8902).

### (D-R6) HIGH — No real User/owner entity; owners are avatar-initial strings (non-unique) disconnected from the team-member list with ids/emails/roles
`OPEN` · **High** · TEAM_MEMBERS :4950; record owner fields (owner:'AM' throughout); settings members table :14001-14006

Real ownership, reassignment, per-user permissions and 'my records' scoping all depend on a User entity with stable ownerId. FIX (large): records reference ownerId; team-member records carry role.

_Evidence:_ 4950 `TEAM_MEMBERS=[{name:'Alex Morgan',avatar:'AM',color:'#3b82f6'},...]` — no id/email/role. Records store `owner:'AM'`/ownerColor; arOwnerColor (2116) resolves color by `u.avatar===code`. Settings members table (14001-14006) has its own members with email+RoleSelect, but record ownership keys off the 2-letter avatar. Two seeded 'Sarah Chen' contacts both use avatar 'SC' — owner strings aren't even unique.

### (D-R7) MEDIUM — Close Won/Lost from deal-detail (ladder / Stage pill) records no reason (deferred)
`OPEN` · **Medium** · DealDetailPage ladder :8047; dStage pill onDealField :8009

Deals closed from the detail page contribute to win/loss counts but have no reason, so DealOutcomeModal reason analytics are incomplete. Explicitly deferred (pipeline-stage-correctness.md line 40). FIX (small): route detail Won/Lost through the same DealOutcomeModal.

_Evidence:_ 8047 ladder Won/Lost `commit=()=>{setStage(s);if(k)updateDeal(d.id,{stageKey:k});}` — no outcome written. 8009 pill sets stageKey directly, no reason, no confirm. Only board-drag (7940) writes deal.outcome.

### (D-R8) MEDIUM — PROP_STAGES, SCHED_DEAL_STAGES, the create/import d_stage field, and the deal Stage FILTER all diverge from canonical DEAL_STAGES_ALL
`OPEN` · **Medium** · PROP_STAGES :14022; SCHED_DEAL_STAGES :18575; d_stage field :2137/:14077; omFieldSchema deal filter :5605

None derive from STAGES_DATA/DEAL_STAGES_ALL. Consequences: scheduling-automation can't set Won/Lost/Prospecting; property builder default omits Prospecting; deal Stage filter can't select Lost. PROP_STAGES/SCHED are explicitly deferred; the create-field and filter omissions are in the same family. The 5605 filter omission predates the waves (commit 60c7ad3), so NOT a wave regression. FIX (small): point all four at DEAL_STAGES_ALL / DEAL_STAGE_KEY_BY_NAME.

_Evidence:_ 14022 `PROP_STAGES=['Qualified','Proposal','Negotiation','Won','Lost']` (omits Prospecting). 18575 `SCHED_DEAL_STAGES=[['prospecting','Prospecting'],['qualified','Qualified'],['proposal','Proposal'],['negotiation','Negotiation']]` (omits Won+Lost, different shape). 2137/14077 d_stage options `['Qualified','Proposal','Negotiation','Won','Lost']` (omits Prospecting → quick-add drawer can't PICK Prospecting). 5605 filter `F('stageName','Stage','select',['Prospecting','Qualified','Proposal','Negotiation','Won'],...)` (omits Lost → can't filter for Lost deals).

### (D-R9) MEDIUM — No shared Reason lookup entity; Won/Lost (and call/flag) reasons are hardcoded per-component arrays
`OPEN` · **Medium** · DealOutcomeModal REASONS :7480-7482; CALL_REASONS/FLAG_REASONS 9456/11224

FIX (small-medium): a shared WIN_REASONS/LOSS_REASONS (ideally Settings-editable) read by the modal, filters and reports. Enables the win/loss analysis Wave 1 aimed at.

_Evidence:_ 7480-7482 `const REASONS=won?['Best fit / value','Competitive price',...]:['Price too high','Chose a competitor',...];` — literals inside the modal. deal.outcome persists the chosen string (7940) but there is no reusable/configurable Reason set for reporting or admin.

### (D-R10) LOW — Changelog narrative overstates auto-created company count (doc says 14 / ids 121-134; current seed yields 9 / ids 121-129)
`PARTIAL` · **Low** · docs/audit/changes/id-based-linking.md line 36; seed STAGES_DATA 7385-7406 + CONTACTS_DATA 4935

Documentation drift only — the code is correct and complete. Flagged so the changelog can be corrected.

_Evidence:_ Doc says '14 companies auto-created (ids 121-134)'. Headless run of migrateLinks over the current seed creates exactly 9 (Northpeak Co, Pivot Studio, Barrett Digital, Apex Labs, Bluewave, Cedar Systems, Quill Media, Vertex Group from deals + Helix Partners from contact Riley Voss co:'Helix Partners'), ids 121-129, 0 orphans.

### (D-R11) LOW — migrateLinks re-runs on every App render (called in function body, not memoized) — pure & harmless, no data corruption
`OPEN` · **Low** · App() :21197; consumed by useState :21202/21204/21209

Pure perf nit, not a correctness bug. Could wrap in useMemo/useRef.

_Evidence:_ 21197 `const _seed=migrateLinks(COMPANIES_DATA,CONTACTS_DATA,dealsSeed());` runs each render, but _seed is referenced only inside useState initializers, which React discards after first mount. Function mutates no module constant (operates on a copy).

