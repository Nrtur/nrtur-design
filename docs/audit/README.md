# nrtur CRM — Gap Audit
_Generated by the crm-gap-audit skill · 2026-07-01 · Phases 1–4 (audit only, no code changed) · source: index.html (21,483 lines)_

Whole-system correctness audit of the CRM data model.

**Status: implemented.** All 14 Critical gaps are now CLOSED (Waves 1–3 + re-audit fixes R1–R5). See [`changes/`](changes/) for the per-fix before/after write-ups and [_REAUDIT-post-wave3.md](_REAUDIT-post-wave3.md) for the re-audit pass. The narrative below is preserved as the original audit record.

## Files
- [_SYSTEM-INVENTORY.md](_SYSTEM-INVENTORY.md) — every entity, field, screen, and cross-entity link (Phase 1)
- [_MODEL-AS-IS.md](_MODEL-AS-IS.md) — the true current entity-relationship map with integrity flags (Phase 2)
- [_GAPS.md](_GAPS.md) — 105 verified gaps, prioritized (Phases 3–4)
- [_USE-CASES.md](_USE-CASES.md) — 14 core workflows traced end-to-end (Phase 4)
- [_ARCHITECTURE.md](_ARCHITECTURE.md) — proposed corrected model (Phase 5, not built)
- `changes/` — before/after docs, one per fix (Phase 7, filled as fixes land)

## The 14 Critical gaps

_All 14 are now ✅ closed (Waves 1–3 + R1–R5) — see [`changes/`](changes/) and [_REAUDIT-post-wave3.md](_REAUDIT-post-wave3.md)._

1. ✅ **"Qualified" is simultaneously a LEAD lifecycle status and a DEAL pipeline stage (the classic lead-vs-deal fusion)** — The single word "Qualified" means two completely different things in this CRM and nothing on screen tells them apart. On a Lead it means "we've decided this prospect is worth pursu
2. ✅ **Leads, Contacts and Companies are wrongly enrolled in pipeline stage boards (pipeline is not Deal-only)** — In a correct CRM the sales pipeline and its stages belong ONLY to Deals (opportunities where money lives). Here the 'New pipeline' dialog offers Deals, Leads, Contacts AND Companie
3. ✅ **deal.pipeline (name string) is never used to scope the pipeline board — multi-pipeline membership is cosmetic** — Each deal stores which pipeline it belongs to as a text field (e.g. 'Sales Pipeline' or 'Onboarding Pipeline'). But the Pipeline board fills its columns purely by matching a deal's
4. ✅ **Eight+ divergent stage/pipeline definitions describe the same conceptual Sales pipeline with different stages and counts (multiple sources of truth)** — There is no single definition of what the sales pipeline's stages are — at least eight independent lists exist and they disagree on the stage names, the number of stages, and even 
5. ✅ **Bulk "Convert" button is a fake action — toasts but creates no Contact/Company/Deal and never marks the lead converted** — When a user selects several leads and clicks the Convert button in the bulk action bar, the app just flashes a message like "3 leads queued for conversion" and does nothing else. N
6. ✅ **Converted records are linked to each other only by copied name-strings, not by id — Contact/Company/Deal share no foreign keys** — When a lead converts, the app creates up to three new records — a Contact, a Company, and a Deal — but it never stores each record's database id on the others. The Contact only rem
7. ✅ **Deals reference companies that don't exist in COMPANIES_DATA (orphaned name-string FK)** — A deal points at its company by copying the company's NAME as plain text, not by a stable id. Eight of the seeded deals name companies that have no matching company record at all —
8. ✅ **Deal 'Company' and 'Primary contact' links resolve against a STATIC constant and by first-contact, not the actual referenced record** — On the deal page, the 'Primary contact' you click does NOT open the person named on the deal — it opens whatever contact happens to be first at that company. And the 'Company' link
9. ✅ **Contacts link to their company by free-text name; several point at non-existent companies** — Each contact stores its employer as a copied text string (contact.co), never a company id. Renaming a company, or a small typo, silently detaches the person from the account. Multi
10. ✅ **Lead conversion joins the three new records (Contact/Company/Deal) only by name-string, never by the ids it just minted** — When a lead is converted, the app creates a Contact, a Company and a Deal in one shot and gives each a fresh id — but it links them to each other using copied names instead of thos
11. ✅ **updateCompany does not cascade a name change, so renaming a company detaches all its children** — Because every child (contacts, deals, invoices, tasks, events, parent links) points at a company by its NAME, and the company-update reducer only patches the company's own row, edi
12. ✅ **Contact record has no real Deals list — the visible "Deals" panel is hardcoded and disconnected from the actual deal derivation** — On every contact's page there is a "Deals" box that always says "Deals · 2" and shows the exact same two fake deals ("Q3 Engagement $24k / Proposal" and "Annual Retainer $48k / Qua
13. ✅ **Win/Loss reason and note are captured then discarded — never persisted on the deal** — When you drag a deal into a Won or Lost stage, a modal asks for a win/loss reason and an optional note. But confirming only moves the deal's stageKey and shows a toast; the reason 
14. ✅ **Contact detail 'Deals' panel is 100% hardcoded — a Contact cannot actually display or hold its deals** — The 'Deals · 2' panel on every Contact shows the same two invented deals (Q3 Engagement $24k, Annual Retainer $48k) and each card's click goes to goTo('deal-detail') with NO id. It
